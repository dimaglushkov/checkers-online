<b>To launch this code you need to install  SDL2.0 on your PC.</b> <br><br>
-On Linux you can do it simply by running following commands in terminal:<br>
<code>
apt-cache search libsdl2 
</code>
<br>
<code>
sudo apt-get install libsdl2-dev
</code>
<br>
